# Current Evaluation State

## Active Text
- **Name:** i_have_no_mouth
- **Attempt:** 14
- **Phase:** awaiting_analysis
- **baseline_score:** 6.35
- **Competitive Mode:** none

## Latest Scores
- Structure Detection: 9/10 ✓
- Character Extraction: 8/10 ✓
  - Completeness: 8/10
  - Identity Resolution: 10/10
  - Alias Grouping: 6/10
- Character Profiles: 6.5/10 ✗
- Chapter Summaries: 4.5/10 ✗
- Pronunciation Guide: 7.5/10 ✗
- HTML Presentation: 8/10 ✓
- **Overall: 7.2/10**

**Pass Criteria:** ALL categories must be >= 8.0
**Status:** FAIL (3 categories below threshold: Character Profiles, Chapter Summaries, Pronunciation Guide)

## What Improved (Attempt 13 → 14)
- **Ted is narrator again** — Fix 1 (self-identification "I am Ted" scan) worked! Ted has `is_narrator=True`.
- **No false positive characters** — "the ice caverns" and "Huergelmir" are GONE. Clean 6-character extraction.
- **All human roles correct** — Ellen, Nimdok, Gorrister, Benny all have `role=protagonist`. AM has `role=antagonist`.
- **Character Extraction passes 8.0** for the first time in several attempts.

## What Regressed or Remains Broken
- **Summary is CATASTROPHICALLY wrong** — LLM summarizer attributed Ted's actions to Gorrister. The plot summary says Gorrister is the narrator, sole survivor, and performs the mercy killings. In reality, TED does all of these things. This is the title of the story: "I Have No Mouth, and I Must Scream" — it's Ted's line.
- **Ted role=minor** — Self-identification scan set `is_narrator=True` but did NOT elevate role from "minor" to "protagonist".
- **AM aliases still missing** — Zero aliases for AM (should have Allied Mastercomputer, Adaptive Manipulator, Aggressive Menace).

## Current Issues (Priority Order)

### CRITICAL
1. **Summary attributes Ted's actions to Gorrister** [Summaries — Factual Error]
   - Problem: Both the plot summary and chapter summary say Gorrister is the narrator, performs the mercy killings, and is the sole survivor transformed into a mouthless blob. ALL of this is Ted.
   - Evidence: Plot summary line 643: "Gorrister recounts the harrowing final journey..." — Wrong. Chapter summary: "leading Gorrister to kill Benny" — Wrong. "leaves Gorrister alive but transforms him into a mouthless, jelly-like entity" — Wrong. Ted kills Benny and the others. Ted is transformed. Ted says "I have no mouth, and I must scream."
   - Root cause: The LLM summarizer (qwen3-next) independently confused Ted and Gorrister. This is NOT caused by narrator detection — summaries are generated by the SummaryAgent, which may not use narrator info. The LLM just got the story wrong.
   - Location: `src/agents/summary_agent.py` or wherever summaries are generated
   - Fix approach: After narrator is identified as Ted, post-process the summary to verify the narrator character is mentioned. If the summary attributes narrator actions (first-person narration, sole survivor) to a DIFFERENT character, flag this as inconsistent. Alternatively: pass the identified narrator name to the summary agent so it can ground the summary correctly.

2. **Ted role=minor instead of protagonist** [Profiles — Wrong Role]
   - Problem: Ted has `is_narrator=True` but `role=minor`. The first-person narrator of the entire story should be `role=protagonist`.
   - Evidence: Ted narrates the entire story, makes the climactic decision to kill his companions as mercy, and is the sole survivor.
   - Root cause: The self-identification scan (Fix 1 from attempt 14) sets `is_narrator=True` but does NOT update the role. The narrator role elevation code in `narrator.py:update_characters_with_narrator()` may only run before the self-identification scan, so the override doesn't cascade to role.
   - Location: `src/agents/characters.py` — wherever STEP 4.24 self-identification scan runs, it should ALSO set `role=protagonist` for the identified narrator.
   - Fix: In the self-identification override code, add `char.role = "protagonist"` alongside setting `is_narrator=True`.

### HIGH
3. **AM has zero aliases** [Extraction — Alias Grouping]
   - Problem: AM has no aliases. The text explicitly states AM stands for "Allied Mastercomputer", "Adaptive Manipulator", "Aggressive Menace", and there's the "cogito ergo sum" / "I think, therefore I AM" reference.
   - Evidence: These acronym expansions are stated in the text. Attempts 11 had them; lost since attempt 12.
   - Root cause: LLM non-determinism in Pass 2.
   - Location: `src/pipeline/character_extraction_v2/main_cast.py` — Pass 2
   - Fix: Programmatic acronym detection: scan text for "{NAME} stood for" / "{NAME} stands for" / "short for {expansion}" patterns near 2-3 letter character names. Inject as aliases deterministically.

4. **6/16 pronunciation entries are common homographs** [Pronunciation — False Positives]
   - Problem: "wind", "read", "lead", "does", "close", "subject" are common English words that most narrators know. 37.5% false positive rate.
   - Evidence: These words appear in virtually every English text. A narrator doesn't need pronunciation guidance for them.
   - Location: Homograph detection in pronunciation pipeline
   - Fix: Add these to a common-homograph exclusion list, or raise the threshold for flagging homographs (only flag if the word appears in an unusual context).

### MEDIUM
5. **Ted's profile refers to "the narrator" instead of "Ted"** [Profiles — Attribution]
   - Problem: Ted's personality description says "the narrator exhibits passivity" instead of "Ted exhibits passivity". Evidence citations also say "The narrator is emotionally detached".
   - Evidence: HTML lines 2012, 2083, 2095, 2107, 2119, 2131
   - Root cause: Profile was generated before narrator identity was established, or the profiler uses "the narrator" generically.
   - Fix: Post-process profiles to replace "the narrator" with the narrator's actual name.

6. **Ted lacks physical description** [Profiles — Completeness]
   - Problem: Ted has no physical_description. In the text, Ted describes himself (paranoid, believes others plot against him). His physical state isn't detailed, but his psychological profile is rich.
   - Evidence: Ted's paranoia and self-importance are central to his characterization.
   - Fix: May auto-resolve if narrator profiling improves.

### LOW
7. **Chapter title is null** [Structure — Cosmetic]
   - Single section with `title: null`. Minor for a short story with no heading.

8. **Relationship labels missing from JSON** [Profiles — Format]
   - Problem: JSON `relationships` are stored as plain strings (just target names), but HTML shows labels correctly (tormentor, companion, captor, romantic interest).
   - Evidence: JSON shows `"Ellen"` not `{"target_name": "Ellen", "relationship": "tormentor"}`.
   - This may be a serialization format difference, not a data issue (HTML has the labels).

## Fix Priority
**CRITICAL #1 (wrong summary)** is the PRIMARY BLOCKER at 4.5/10. The summary is factually wrong about who the narrator/survivor/killer is.

**CRITICAL #2 (Ted role=minor)** is a quick fix — just add role elevation to the self-identification override.

**HIGH #3 (AM aliases)** and **HIGH #4 (pronunciation)** are needed to push those categories above 8.0.

## Score History
| Attempt | Score | Delta from Baseline | Notes |
|---------|-------|---------------------|-------|
| 1 | 6.35 | 0 | Baseline - duplicate Benny, wrong narrator, profile errors |
| 2 | 7.3 | +0.95 | Benny dedup fixed, narrator=Ted, but duplicate Ted appeared, profiles improved |
| 3 | 7.8 | +1.45 | Relationship vocab improved, pronunciation fixed, but duplicate Ted and AM role fixes didn't work |
| 4 | 7.6 | +1.25 | Fixes did NOT take effect — duplicate Ted persists, AM still "protagonist" |
| 5 | 8.1 | +1.75 | Dup Ted FIXED, AM now antagonist, self-alias fixed. But 3 humans wrongly labeled antagonist |
| 6 | 8.4 | +2.05 | ACTIVE/PASSIVE fix did NOT work — semantic direction bug. 4 humans still "antagonist" |
| 7 | 6.65 | +0.30 | REGRESSION: Ted missing (replaced by "the ice caverns"), 4 humans still antagonist |
| 8 | 8.50 | +2.15 | Ted restored, all roles correct. Only profiles failing (AM "colleague" labels) |
| 9 | 7.25 | +0.90 | REGRESSION: Ellen detected as narrator (not Ted). Colleague fix partially worked but narrator cascade drops 3 categories |
| 10 | 7.65 | +1.30 | Ted narrator restored. But 3 humans still antagonist, colleague labels persist, summary uses "the narrator" not "Ted" |
| 11 | 8.30 | +1.95 | Major progress: Ellen/Benny protagonist, AM relationships fixed, aliases added, summary uses "Ted". Only Nimdok antagonist remains. |
| 12 | 8.20 | +1.85 | Nimdok FIXED to protagonist. But LLM regression: AM aliases lost, ice caverns spurious character, Ted role="minor" |
| 13 | 7.23 | +0.88 | REGRESSION: "the ice caverns" is narrator, Ted not narrator, Huergelmir false positive, Gorrister antagonist |
| 14 | 7.20 | +0.85 | Ted narrator restored, no false positives. But summary CATASTROPHICALLY wrong (Gorrister≠Ted), Ted role=minor |

## Pipeline Notes (Attempt 14)
- Analysis completed in 22m 20s
- 6 characters found (down from 8 in attempt 13 — false positives removed!)
- Ted: is_narrator=True, role=minor — narrator detection works, role elevation doesn't
- Gorrister: role=protagonist — correctly NOT antagonist (Fix 2 threshold worked)
- AM: role=antagonist, zero aliases
- Summary LLM confused Ted and Gorrister — attributed all of Ted's actions to Gorrister
- Model: qwen3-next:80b-a3b-instruct-q8_0 (all agents)

## Fix History
- Attempt 2: Three connected fixes for character extraction and pronunciation
  1. **Exact-name dedup in `_merge_within_main_cast`** (`src/agents/characters.py` Pass -1) — Fixed Benny duplicate
  2. **Vocative pattern + narrator fallback** (`src/agents/characters.py`) — Fixed narrator detection but introduced duplicate Ted
  3. **Pronunciation fixes** (`cmu_proposer.py`, `enricher.py`) — Partially fixed false positives

- Attempt 3: Four fixes (two didn't take effect)
  1. **STEP 5.8.5b same-name guard** (`src/agents/characters.py`) — Code is correct but targets wrong path; duplicate Ted comes from STEP 5.8.6
  2. **Post-profile adversarial role correction** (`src/analyzer.py`) — Code is correct but `_ADVERSARIAL_LABELS` doesn't include "victim"
  3. **Relationship vocabulary expanded** (`src/analyzer.py`) — WORKED: but LLM still uses "colleague" as fallback
  4. **Pronunciation whitelist additions** (`cmu_proposer.py`) — WORKED

- Attempt 4: Three fixes — **NONE took effect on the actual problem**
  1. **STEP 5.8 same-name dedup** (`src/agents/characters.py:1476-1494`) — Targeted supporting->main promotion, but duplicate Ted comes from STEP 5.8.6 narrator fallback
  2. **"victim" added to `_ADVERSARIAL_LABELS`** (`src/analyzer.py:2134`) — Correct addition, but AM's outgoing labels are mostly "colleague" not "victim", so threshold not met
  3. **Self-relationship filter** (`src/analyzer.py`) — May have worked (no self-relationships visible), but didn't address core issues

- Attempt 5: Four fixes — **3 of 4 worked**
  1. **STEP 5.2b placeholder->existing merge** (`src/agents/characters.py`) — WORKED: No duplicate Ted
  2. **Incoming adversarial label check** (`src/analyzer.py`) — WORKED: AM now correctly "antagonist"
  3. **False antagonist correction** (`src/analyzer.py`) — PARTIALLY WORKED: Benny fixed, Ellen/Nimdok/Gorrister still "antagonist" because they have adversarial-looking labels (enemy, victim)
  4. **Self-alias filter** (`src/agents/characters.py:_is_valid_alias`) — WORKED: No AM self-alias

- Attempt 6: Two fixes — **Neither took effect**
  1. **ACTIVE vs PASSIVE adversarial labels** (`src/analyzer.py:2177-2213`) — Code correctly separates label sets, BUT has semantic direction bug
  2. **Consistency enforcement for colleague labels** (`src/analyzer.py:2215-2285`) — Depends on correct role assignment which didn't happen

- Attempt 7: Direction-aware aggressor labels fix — **Did NOT fix roles**
  1. **Direction-aware _OUTGOING_AGGRESSOR_LABELS + _INCOMING_AGGRESSOR_LABELS** (`src/analyzer.py:2184-2223`) — threshold too strict

- Attempt 8: Two robustness fixes — **BOTH WORKED**
  1. **STEP 4.25b: narrator vocative check expansion** (`src/agents/characters.py:829-874`) — Ted restored as narrator. Fixed.
  2. **False-antagonist threshold raised** (`src/analyzer.py:2218`) — All 4 humans now correctly "protagonist". Fixed.

- Attempt 9: Colleague label replacement for antagonist<->protagonist relationships
  1. **Post-profile colleague->role-appropriate label replacement** (`src/analyzer.py`) — PARTIALLY WORKED
  2. **REGRESSION**: Ellen detected as narrator instead of Ted — LLM non-determinism

- Attempt 10: Two fixes
  1. **STEP 4.27 mention-ratio narrator validation** (`src/agents/characters.py`) — WORKED: Ted is narrator again
  2. **role="main" in _all_protagonists** (`src/analyzer.py`) — DID NOT WORK

- Attempt 11: Two fixes — **MOSTLY WORKED**
  1. **Post-Phase-B role correction + colleague replacement** (`src/analyzer.py`) — WORKED for Ellen/Benny
  2. **Summary narrator name substitution** (`src/analyzer.py`) — WORKED

- Attempt 12: One fix — **WORKED but LLM regression on other fronts**
  1. **"fellow victim" guard in post-Phase-B false-antagonist check** (`src/analyzer.py`) — WORKED

- Attempt 13: Two fixes — **Neither took effect due to LLM variation**
  1. **Narrator role elevation** (`narrator.py`) — Didn't fire (Ted wasn't flagged as narrator)
  2. **Possessive sub-entity filter** (`characters.py`) — Didn't fire (LLM used non-possessive form)

- Attempt 14: Two fixes — **PARTIAL SUCCESS**
  1. **STEP 4.24 self-identification scan** (`characters.py`) — WORKED: Ted is narrator. But role not elevated.
  2. **Post-Phase-B threshold `_fc_own <= 2`** (`analyzer.py`) — WORKED: Gorrister now protagonist.

## Modification History

| Attempt | Issue | Files Modified | Result |
|---------|-------|----------------|--------|
| 2 | Dup Benny | characters.py (Pass -1 dedup) | Fixed |
| 2 | Wrong narrator | characters.py (vocative + STEP 4.5b) | Fixed but introduced dup Ted |
| 2 | Pronunciation FPs | cmu_proposer.py, enricher.py | Partially fixed |
| 3 | Dup Ted | characters.py (STEP 5.8.5b same-name guard) | No change — wrong code path |
| 3 | AM wrong role | analyzer.py (post-profile adversarial role correction) | No change |
| 3 | Relationship vocab | analyzer.py | Fixed vocab |
| 3 | Pronunciation FPs | cmu_proposer.py | Fixed |
| 4 | Dup Ted | characters.py (STEP 5.8 promotion dedup) | No change |
| 4 | AM wrong role | analyzer.py | No change |
| 5 | Dup Ted | characters.py (STEP 5.2b placeholder merge) | **Fixed** |
| 5 | AM wrong role | analyzer.py (incoming adversarial check) | **Fixed** |
| 5 | False antagonist | analyzer.py | **Partial** |
| 5 | AM self-alias | characters.py | **Fixed** |
| 6 | Wrong roles | analyzer.py (ACTIVE vs PASSIVE labels) | **No change** |
| 7 | Wrong roles | analyzer.py (direction-aware labels) | **No change** |
| 8 | Ted missing | characters.py (STEP 4.25b vocative expansion) | **Fixed** |
| 8 | Wrong roles | analyzer.py (threshold <=1) | **Fixed** |
| 9 | Colleague labels | analyzer.py (post-profile colleague replacement) | **Partial** |
| 10 | Narrator fix | characters.py (STEP 4.27 mention-ratio guard) | **Fixed** |
| 11 | Roles + colleagues | analyzer.py (post-Phase-B re-run) | **Partial** |
| 11 | Summary narrator | analyzer.py (name substitution) | **Fixed** |
| 12 | Nimdok antagonist | analyzer.py ("fellow victim" guard) | **Fixed** |
| 13 | Narrator elevation | narrator.py | **No change** |
| 13 | Possessive filter | characters.py | **No change** |
| 14 | Self-identification | characters.py (STEP 4.24) | **Fixed** (narrator only, not role) |
| 14 | Antagonist threshold | analyzer.py (_fc_own <= 2) | **Fixed** |

## Key Debugging Notes for Fix Phase

**The PRIMARY blocker is now SUMMARIES (4.5/10), not character extraction.**

1. **CRITICAL: Summary factual errors.** The LLM summarizer confused Ted and Gorrister. This is independent of narrator detection — the LLM just generated a wrong summary. Fix options:
   - **Post-summary narrator consistency check:** After narrator is identified, verify the summary mentions the narrator character in narrator-appropriate roles (survivor, first-person actions). If another character is attributed those roles, flag/correct.
   - **Pass narrator name to summary prompt:** If the SummaryAgent receives the narrator name, it can ground the summary. Check if narrator info is available when summaries are generated.
   - **Post-summary name substitution:** If the summary says "Gorrister recounts" but Ted is the identified narrator, substitute.

2. **CRITICAL: Ted role=minor.** The self-identification scan sets `is_narrator=True` but doesn't set `role="protagonist"`. Add one line to the fix.

3. **HIGH: AM aliases.** Programmatic acronym detection would be deterministic.

4. **HIGH: Pronunciation false positives.** Common homographs need exclusion.

## Output Files
- HTML: ../output/i_have_no_mouth/report.html
- JSON: ../output/i_have_no_mouth/analysis.json

## Next Action
Re-run analysis to verify fixes.

## Fix History (continued)
- Attempt 15 fixes:
  1. **SINGLE_CHAPTER_PROMPT narrator guideline** (`summarizer.py:175`) — Added `**FIRST-PERSON NARRATORS**` guideline matching what already exists in CHUNK_SUMMARY_PROMPT and CONSOLIDATE_PROMPT. Short stories use this path; it was missing.
     - Root cause: Inconsistency — multi-chunk paths had the guideline, single-chunk path didn't
     - Smoke test: PASS — guideline now present in all 3 prompt paths
  2. **Narrator role invariant STEP 5.9.6** (`characters.py:after STEP 5.9.5`) — Final post-all-merges invariant: if is_narrator=True and pov=first-person, role must be protagonist.
     - Root cause: Ted had is_narrator=True but role=minor; nothing guaranteed role elevation after all merge/split steps
     - Smoke test: PASS — invariant added after STEP 5.9.5 which already skips narrators
  3. **Acronym alias injection STEP 1.2** (`characters.py:after STEP 1`) — Scans raw text for "{CAPS} stands/stood for {Expansion}" and "{CAPS}. Expansion" patterns; injects as aliases for all-caps 2-5 letter character names.
     - Root cause: LLM non-determinism in Pass 2 would sometimes miss AM's aliases; programmatic detection is deterministic
     - Smoke test: PASS — correctly finds "Allied Mastercomputer" and "Aggressive Menace" from test text
  4. **Common homograph exclusion** (`homograph_proposer.py`) — Added "wind", "read", "lead", "does", "close", "subject" to COMMON_HOMOGRAPHS_EXCLUSION + added the check to the propose() loop (was defined but never checked).
     - Root cause: These 6 words were in HOMOGRAPHS dict; exclusion set existed but was never applied
     - Smoke test: PASS — words excluded from proposals; test updated to use non-excluded words
     - Tests: all 332 pass
